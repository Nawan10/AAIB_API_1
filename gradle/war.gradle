apply plugin: "war"

bootWar {
    mainClass = "lk.geoedge.interoperability.Aaibapi1App"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
