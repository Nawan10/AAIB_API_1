package lk.geoedge.interoperability.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class CropVarietyCropTypeAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCropVarietyCropTypeAllPropertiesEquals(CropVarietyCropType expected, CropVarietyCropType actual) {
        assertCropVarietyCropTypeAutoGeneratedPropertiesEquals(expected, actual);
        assertCropVarietyCropTypeAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCropVarietyCropTypeAllUpdatablePropertiesEquals(CropVarietyCropType expected, CropVarietyCropType actual) {
        assertCropVarietyCropTypeUpdatableFieldsEquals(expected, actual);
        assertCropVarietyCropTypeUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCropVarietyCropTypeAutoGeneratedPropertiesEquals(CropVarietyCropType expected, CropVarietyCropType actual) {
        assertThat(actual)
            .as("Verify CropVarietyCropType auto generated properties")
            .satisfies(a -> assertThat(a.getId()).as("check id").isEqualTo(expected.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCropVarietyCropTypeUpdatableFieldsEquals(CropVarietyCropType expected, CropVarietyCropType actual) {
        assertThat(actual)
            .as("Verify CropVarietyCropType relevant properties")
            .satisfies(a -> assertThat(a.getCrop()).as("check crop").isEqualTo(expected.getCrop()))
            .satisfies(a -> assertThat(a.getImage()).as("check image").isEqualTo(expected.getImage()))
            .satisfies(a -> assertThat(a.getMainCrop()).as("check mainCrop").isEqualTo(expected.getMainCrop()))
            .satisfies(a -> assertThat(a.getCropCode()).as("check cropCode").isEqualTo(expected.getCropCode()))
            .satisfies(a -> assertThat(a.getNoOfStages()).as("check noOfStages").isEqualTo(expected.getNoOfStages()))
            .satisfies(a -> assertThat(a.getDescription()).as("check description").isEqualTo(expected.getDescription()))
            .satisfies(a -> assertThat(a.getCropTypesId()).as("check cropTypesId").isEqualTo(expected.getCropTypesId()))
            .satisfies(a -> assertThat(a.getUnitsId()).as("check unitsId").isEqualTo(expected.getUnitsId()))
            .satisfies(a -> assertThat(a.getArea()).as("check area").isEqualTo(expected.getArea()))
            .satisfies(a -> assertThat(a.getSumInsured()).as("check sumInsured").isEqualTo(expected.getSumInsured()))
            .satisfies(a -> assertThat(a.getMinSumInsured()).as("check minSumInsured").isEqualTo(expected.getMinSumInsured()))
            .satisfies(a -> assertThat(a.getMaxSumInsured()).as("check maxSumInsured").isEqualTo(expected.getMaxSumInsured()))
            .satisfies(a ->
                assertThat(a.getSubsidisedPremiumRate()).as("check subsidisedPremiumRate").isEqualTo(expected.getSubsidisedPremiumRate())
            );
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCropVarietyCropTypeUpdatableRelationshipsEquals(CropVarietyCropType expected, CropVarietyCropType actual) {
        // empty method
    }
}
